// Criando um array vazio inicialmente
let arraySimples = [];

// Adicionando os números 10, 20 e 30 ao array usando o método push()
arraySimples.push(10);
arraySimples.push(20);
arraySimples.push(30);

// Imprimindo o array resultante no console
console.log("Array resultante (Simples):");
console.log(arraySimples);

